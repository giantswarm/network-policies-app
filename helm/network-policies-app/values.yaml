name: network-policies-app
serviceType: managed

project:
  branch: "[[ .Branch ]]"
  commit: "[[ .SHA ]]"

denyByDefault:
  enabled: true
  namespaces:
  - kube-system
  - giantswarm

allowEgressToDNS:
  enabled: true
  namespaces:
  - kube-system
  - giantswarm
