name: network-policies-app
serviceType: managed

project:
  branch: "[[ .Branch ]]"
  commit: "[[ .SHA ]]"

extraPolicies:
  allowGlobalEgressToCoreDNS:
    enabled: true

denyByDefault:
  enabled: true
  namespaces:
  - kube-system
  - giantswarm
